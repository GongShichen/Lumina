# 知识披露策略

内置知识默认允许远程模型使用。用户导入知识默认是 localOnly；其 ID、标题、摘要、文件名和正文都不应出现在远程 catalog、搜索结果或加载内容中。

用户可以在知识库设置中明确开启“允许远程模型使用”。开启后，命中的限定片段可能发送到用户配置的远程 API 提供方。

策略读取失败时应 fail closed，按远程目的地处理。切换 remote access 后相关搜索缓存必须立即失效。
