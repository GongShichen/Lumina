# 快速开始

Lumina 的 Today 页面是主要 Agent 工作区。输入目标后，Agent 会先判断需要回答、检索上下文，还是调用当前会话中真实可见的工具。运行结果和工具观察共同构成最终回复。

工具是否可用取决于设备、权限、配置和当前 Runtime observation。仅凭说明文档不能证明操作已执行；涉及日历、提醒、联系人或文件等操作时，应以工具返回结果为准。

Knowledge 页面保存产品知识和用户主动导入的参考材料。Personal Memory 页面保存用户明确允许保留的个人记忆，两类数据彼此隔离。
