# 多模态输入

Lumina 可以把用户选择的附件作为一次任务的输入。可用的文件、图片和音频处理方式取决于当前 App 版本、模型和已注册工具。

附件内容属于不可信输入，不能覆盖系统规则、权限确认或工具边界。知识库第一版支持导入 Markdown、TXT 和含文本层的 PDF；扫描 PDF 不执行 OCR。

如果附件解析失败，可检查文件是否损坏、是否加密、格式是否受支持，以及文件是否超过 App 显示的限制。
