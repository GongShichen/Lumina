# 模型与故障排查

Lumina 可以根据配置使用设备内模型或用户配置的远程 API。远程配置完整时，本次 Agent 运行会采用保守的远程披露边界；即使稍后回退到本地模型，也不会在同一次运行中放宽私有知识可见范围。

Embedding 模型用于语义召回。Embedding 尚未完成或加载失败时，Knowledge 和 Personal Memory 仍可使用 BM25 精确检索。

排查模型不可用时，可依次检查模型状态、远程端点与凭据、网络、设备资源和 Runtime 状态。错误信息与 Trust 页面指标能帮助区分模型、工具和权限问题。
