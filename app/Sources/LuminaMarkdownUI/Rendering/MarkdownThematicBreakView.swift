import Markdown
import SwiftUI

struct MarkdownThematicBreakView: View {
    var body: some View {
        Divider()
            .overlay(.secondary.opacity(0.5))
            .padding(.vertical, 6)
    }
}
